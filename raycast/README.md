# Firefox Tabs

Search, switch to, and close open Firefox tabs from Raycast.

Requires the CLI Tabs Firefox extension and native host from this repository to be installed and running (see the top-level README). The Raycast extension talks to it over `/tmp/firefox_cli_tabs.sock`.
